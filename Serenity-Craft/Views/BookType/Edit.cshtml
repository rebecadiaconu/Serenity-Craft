@model Serenity_Craft.Models.BookType
@{
    ViewBag.Title = "Update";
}

<h2>@ViewBag.Title</h2>

@if (ViewBag.Message != null)
{
    <div class="alert alert-danger ">@ViewBag.Message</div>
}

@using (Html.BeginForm("Edit", "BookType"))
{
    @Html.HttpMethodOverride(HttpVerbs.Put)

    @Html.HiddenFor(b => b.BookTypeId)
    <br />

    @Html.Label("Name", "Book type name:")
    <br />
    @Html.EditorFor(b => b.Name, new { placeholder = "Type in the book type's name", @class = "form-control" })
    @Html.ValidationMessageFor(b => b.Name, "", new { @class = "text-danger" })
    <br />
    <br />

    <button class="btn btn-primary" type="submit">Update</button>
}
